/* Tab-Leiste — bewusst OHNE Verhalten.

   Bis 02.08.2026 schrumpfte die Leiste hier beim Runterscrollen zur Icon-Kapsel
   (Klasse `tabbar--min`) und musste die Auswahl-Pille danach neu vermessen.
   Beides ist entfernt: die Leiste hat jetzt eine feste Höhe, damit die
   Unterkante beim Scrollen und beim Tab-Wechsel still steht.

   Die Datei bleibt als letztes Skript in index.html stehen (Ladereihenfolge),
   falls die Leiste später wieder eigenes Verhalten bekommt. */
